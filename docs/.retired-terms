# Retired false-claim phrases. Decision-log entries are exempt from this guard.
unavailable server|Codex server|server address|local-server|App-server :: Codex usage is a direct provider response contract, not a server integration.
