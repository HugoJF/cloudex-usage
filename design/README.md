# Design Artifacts

Aura design-system and design-screen outputs live here. A reference becomes a build
target only after its canonical variant is explicitly identified.

- [`direction-lab`](direction-lab/) — executable Direction D primitive catalog and
  the preserved A–D exploration record.
- [`system/tokens.json`](system/tokens.json) — canonical GNOME visual tokens.
- [`captures`](captures/) — J-001 visual evidence generated from the packaged catalog.
